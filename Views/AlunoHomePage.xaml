<Window x:Class="AlunoHomePage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SIGAWPF"
        mc:Ignorable="d"
        Title="AlunoHomePage" Height="450" Width="800">
    <Grid>
        <TabControl>
            <TabItem Header="Disciplinas">
                <Grid Background="#FFE5E5E5">
                    <DataGrid x:Name="DisciplinasAlunoDataGrid" Margin="66,58,328,81">
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Ver disciplina">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Click="VerDisciplina_Click" Tag="{Binding Id}" Content="Ver"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="Notas">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
